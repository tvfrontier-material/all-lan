using System; // Imports the System namespace, providing access to fundamental classes like Console.

namespace HelloWorldApp // Declares a namespace to organize related code.
{
    class Program // Defines a class named 'Program', which is a blueprint for objects.
    {
        static void Main(string[] args) // The entry point of the application.
        {
            // This line prints "Hello, World!" to the console.
            Console.WriteLine("Hello, World!"); 
        }
    }
}
